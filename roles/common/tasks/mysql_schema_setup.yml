- name: Install MySQL Client
  ansible.builtin.yum:
    name: mysql
    state: installed

- name: load Schema
  ansible.builtin.shell: mysql -h mysql-dev.sandeepreddymunagala123.xyz -uroot -pRoboShop@1 </app/schema/{{ component }}.sql






